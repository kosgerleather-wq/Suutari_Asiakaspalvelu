# Tehtaankatu Suutari – Atelier Flow V7

Data model: Request ID (S/W/E/P), source, Customer ID, created_at, product, service, status, price and Job ID.

Reports & Export: source/status breakdown, conversion rate, revenue, filters, CSV export and Excel-compatible .xls export.

Production next step: replace demo arrays with a real database/API while keeping these fields stable.
